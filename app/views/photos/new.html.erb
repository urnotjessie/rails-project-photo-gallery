<%= form_for @photo do |f| %>
  <%= f.file_field :image %>
  <%= f.text_field :caption, placeholder: "Add Caption"%>
  <%= f.select :user_id, options_from_collection_for_select(User.all, :id, :username) %>

  <%= f.submit "Upload photo"%>
<% end %>
